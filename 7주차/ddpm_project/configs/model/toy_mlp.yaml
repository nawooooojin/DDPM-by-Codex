_target_: src.models.lit_classifier.LitClassifier
net:
  _target_: src.models.toy_mlp.ToyMLP
  input_dim: ${data.input_dim}
  hidden_dim: 64
  num_classes: ${data.num_classes}
  dropout: 0.1
lr: 0.001
weight_decay: 0.0
num_classes: ${data.num_classes}
